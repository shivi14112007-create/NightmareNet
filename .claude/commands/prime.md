Load full NightmareNet project context for a productive session.

Read these files in order to prime your understanding:

1. **CLAUDE.md** — Project instructions, conventions, and gotchas
2. **pyproject.toml** — Dependencies, tool config, entry points
3. `nightmarenet/__init__.py` — Package version and exports
4. **nightmarenet/api/app.py** (first 80 lines) — API structure and routes
5. **nightmarenet/api/schemas.py** — Request/response models
6. **frontend/src/lib/api.ts** — Frontend API client (mirrors backend)
7. **tests/** directory listing — Test file inventory

After reading, provide a brief status report:
- Python package version
- Number of test files
- API endpoints available
- Any immediate issues visible (import errors, syntax problems)

Do NOT make any changes. This is read-only context loading.
